# dependencies
node_modules/

# next.js
.next/
out/

# production
build/

# env files
.env
.env.*
!.env.example
.envrc
.env*.local
.dev.vars

# data
data/

# runtime-generated artifacts (build-time from ABI / plugin scan)
.tongflow/
.openflow/

# local plugins are managed as separate repos
plugins/tongflow/
plugins/tongflow-llm-*/
plugins/tongflow-modal-*/

# local feature registry overrides (see docs/feature-registry.md)
.tongflow/features.local.json

# misc
.DS_Store
*.tsbuildinfo
next-env.d.ts

# local Python venv (optional, e.g. pip install modal)
.venv/

# removed Electron / old desktop tooling — ignore if folders still exist locally
wheelhouse/
dist-electron/
release/
electron-out/

plugins/
