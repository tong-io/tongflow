/**
 * Minimum supported `version` integer in `config/tongflow.abi.json`.
 * Shared by runtime parse (`tongflow-abi.ts`) and build-time codegen (`gen-abi-types.ts`).
 */
export const MIN_SUPPORTED_ABI_VERSION = 1;
